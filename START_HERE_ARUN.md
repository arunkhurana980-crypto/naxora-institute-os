# Start Here Arun - Part 48

1. Open backend folder.
2. Paste your working `.env`.
3. Run `npm install` then `npm run dev`.
4. Open `http://127.0.0.1:5000/app/razorpay-final.html`.

Razorpay keys missing honge to mock mode active rahega; app crash nahi karega.
