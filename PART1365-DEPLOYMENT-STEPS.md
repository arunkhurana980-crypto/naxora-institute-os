# Deployment

```powershell
git status
git add .
git commit -m "Fix Owner app route and unified session launch"
git push
```

Render:

```text
Manual Deploy
→ Clear build cache & deploy
```

Test:

- `/api/part1365/status`
- `/owner-login`
- `/app`
- `/app/`
