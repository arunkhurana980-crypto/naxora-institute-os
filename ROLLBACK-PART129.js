import fs from "node:fs";import path from "node:path";import{fileURLToPath}from"node:url";import{spawnSync}from"node:child_process";
const root=path.dirname(fileURLToPath(import.meta.url)),serverPath=path.join(root,"backend/src/server.js"),shellPath=path.join(root,"backend/src/part119-unified-app-shell.js"),htmlPath=path.join(root,"frontend/naxora-unified-app.html");const marker="// ================= PART 129 — VANI BULK CSV JSON IMPORT =================",ms="  // PART 129 VANI BULK IMPORT MODULE START",me="  // PART 129 VANI BULK IMPORT MODULE END",as="  // PART 129 VANI BULK IMPORT ALIASES START",ae="  // PART 129 VANI BULK IMPORT ALIASES END";function remove(source,start,end){const a=start.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),b=end.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");return source.replace(new RegExp(`\\s*${a}[\\s\\S]*?${b}\\s*`,"g"),"\n")}const original={server:fs.readFileSync(serverPath,"utf8"),shell:fs.readFileSync(shellPath,"utf8"),html:fs.readFileSync(htmlPath,"utf8")};const escaped=marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");let server=original.server.replace(new RegExp(`\\n*${escaped}\\nconst \\{ registerPart129VaniBulkImport \\} = await import\\("\\.\\/part129-vani-bulk-import\\.js"\\);\\nregisterPart129VaniBulkImport\\(\\{ app \\}\\);\\n*`,"g"),"\n\n");let shell=remove(remove(original.shell,ms,me),as,ae);let html=original.html.replace(/\s*<article><strong>129<\/strong><span>VANI bulk import<\/span><\/article>/g,"").replace("Part 129 VANI CSV/JSON bulk import active hai: mapping, validation, duplicate control, confirmed execution aur rollback.","Part 128 VANI master-data actions active hain. Bulk import Part 129 me hoga.").replace("Part 129 • 9 operational + 14 master-data + CSV/JSON bulk import","Part 128 • 9 operational + 14 master-data VANI actions").replace(/\s*<script src="\/naxora-part129-global-vani-bridge\.js"><\/script>/g,"");fs.writeFileSync(serverPath,server);fs.writeFileSync(shellPath,shell);fs.writeFileSync(htmlPath,html);for(const file of[serverPath,shellPath]){const result=spawnSync(process.execPath,["--check",file],{encoding:"utf8"});if(result.status!==0){fs.writeFileSync(serverPath,original.server);fs.writeFileSync(shellPath,original.shell);fs.writeFileSync(htmlPath,original.html);console.error("Part 129 rollback syntax failed; originals restored.");process.exit(1)}}console.log("Part 129 registration, shell module, aliases, bridge and progress removed.");
