import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { spawnSync } from "node:child_process";
const root=path.dirname(fileURLToPath(import.meta.url)),serverPath=path.join(root,"backend/src/server.js"),shellPath=path.join(root,"backend/src/part119-unified-app-shell.js"),htmlPath=path.join(root,"frontend/naxora-unified-app.html"),marker="// ================= PART 130 — VANI ACADEMIC OPERATIONS =================",moduleStart="  // PART 130 VANI ACADEMIC OPERATIONS MODULE START",moduleEnd="  // PART 130 VANI ACADEMIC OPERATIONS MODULE END",aliasStart="  // PART 130 VANI ACADEMIC OPERATIONS ALIASES START",aliasEnd="  // PART 130 VANI ACADEMIC OPERATIONS ALIASES END";
function removeBetween(source,start,end){const a=start.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),b=end.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");return source.replace(new RegExp(`\\s*${a}[\\s\\S]*?${b}\\s*`,"g"),"\n")}
const original={server:fs.readFileSync(serverPath,"utf8"),shell:fs.readFileSync(shellPath,"utf8"),html:fs.readFileSync(htmlPath,"utf8")};const escaped=marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");const server=original.server.replace(new RegExp(`\\n*${escaped}\\nconst \\{ registerPart130VaniAcademicOperations \\} = await import\\("\\.\\/part130-vani-academic-operations\\.js"\\);\\nregisterPart130VaniAcademicOperations\\(\\{ app \\}\\);\\n*`,"g"),"\n\n");let shell=removeBetween(original.shell,moduleStart,moduleEnd);shell=removeBetween(shell,aliasStart,aliasEnd);let html=original.html.replace(/\s*<article><strong>130<\/strong><span>VANI academics<\/span><\/article>/g,"").replace("Part 130 VANI academic operations active hain: timetable, bulk attendance, assignments, exams, marks, results aur Student progress.","Part 129 VANI CSV/JSON bulk import active hai: mapping, validation, duplicate control, confirmed execution aur rollback.").replace("Part 130 • 9 operational + 14 master-data + bulk import + 12 academic actions","Part 129 • 9 operational + 14 master-data + CSV/JSON bulk import").replace(/\s*<script src="\/naxora-part130-global-vani-bridge\.js"><\/script>/g,"");fs.writeFileSync(serverPath,server,"utf8");fs.writeFileSync(shellPath,shell,"utf8");fs.writeFileSync(htmlPath,html,"utf8");for(const file of [serverPath,shellPath]){const result=spawnSync(process.execPath,["--check",file],{encoding:"utf8"});if(result.status!==0){fs.writeFileSync(serverPath,original.server,"utf8");fs.writeFileSync(shellPath,original.shell,"utf8");fs.writeFileSync(htmlPath,original.html,"utf8");console.error("Part 130 rollback syntax failed; original files restored.");process.exit(1)}}console.log("Part 130 registration, shell module, aliases, bridge and progress removed.");