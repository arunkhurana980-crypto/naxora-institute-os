# Deployment

```powershell
git status
git add .
git commit -m "Add simple Owner signup and login"
git push
```

Render:

```text
Manual Deploy
→ Clear build cache & deploy
```

Test:

- `/owner-login`
- `/create-institute`
- `/common-login`
- `/api/part1364/status`
