# START HERE — Part 74

1. Extract ZIP.
2. Copy all files into old live project folder.
3. Replace files.
4. Run:

```bash
git status
git add .
git commit -m "Add Part 74 AI Parent Communication Weekly Summary"
git push
```

5. Render → Manual Deploy → Clear build cache & deploy.
6. Test:
- `/api/part74/status`
- `/ai-parent-weekly-summary`
- `/api/part74/demo`

No `.env`, no secrets, no `.bat`.
