# NAXORA Final 136.12 — Role VANI Cache & UX Fixed

This replaces the previous 136.11 ZIP.

Screenshot fix:

- Final Role VANI JavaScript/CSS forced to version `13612`.
- Frontend assets are delivered with `no-store`.
- Empty command is not treated as a broken VANI action.
- Action dropdown selection clears old command errors.
- A separate clear-message button is available.

One command:

```powershell
node .\APPLY-NAXORA-FINAL.js
```
