# Test

Open `/api/part1361/status` and confirm `secretTransportFixActive: true`. On `/owner-bootstrap`, click Generate Preview. The page first verifies the secret, then fills Institute ID and exact confirmation.
