import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { spawnSync } from "node:child_process";
const root=path.dirname(fileURLToPath(import.meta.url));const serverPath=path.join(root,"backend/src/server.js"),shellPath=path.join(root,"backend/src/part119-unified-app-shell.js"),htmlPath=path.join(root,"frontend/naxora-unified-app.html");const marker="// ================= PART 131 — VANI FEES FINANCE OPERATIONS =================",moduleStart="  // PART 131 VANI FEES FINANCE MODULE START",moduleEnd="  // PART 131 VANI FEES FINANCE MODULE END",aliasStart="  // PART 131 VANI FEES FINANCE ALIASES START",aliasEnd="  // PART 131 VANI FEES FINANCE ALIASES END";function removeBetween(source,start,end){const a=start.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),b=end.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");return source.replace(new RegExp(`\\s*${a}[\\s\\S]*?${b}\\s*`,"g"),"\n")}const original={server:fs.readFileSync(serverPath,"utf8"),shell:fs.readFileSync(shellPath,"utf8"),html:fs.readFileSync(htmlPath,"utf8")};const escaped=marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&");const server=original.server.replace(new RegExp(`\\n*${escaped}\\nconst \\{ registerPart131VaniFeesFinanceOperations \\} = await import\\("\\.\\/part131-vani-fees-finance-operations\\.js"\\);\\nregisterPart131VaniFeesFinanceOperations\\(\\{ app \\}\\);\\n*`,"g"),"\n\n");let shell=removeBetween(original.shell,moduleStart,moduleEnd);shell=removeBetween(shell,aliasStart,aliasEnd);let html=original.html.replace(/\s*<article><strong>131<\/strong><span>VANI finance<\/span><\/article>/g,"").replace("Part 131 VANI fees and finance operations active hain: fee structures, Student fees, invoices, manual receipts, due lists, reminders aur finance summaries.","Part 130 VANI academic operations active hain: timetable, bulk attendance, assignments, exams, marks, results aur Student progress.").replace("Part 131 • operational + master-data + bulk import + academics + 12 finance actions","Part 130 • 9 operational + 14 master-data + bulk import + 12 academic actions").replace(/\s*<script src="\/naxora-part131-global-vani-bridge\.js"><\/script>/g,"");fs.writeFileSync(serverPath,server,"utf8");fs.writeFileSync(shellPath,shell,"utf8");fs.writeFileSync(htmlPath,html,"utf8");for(const file of [serverPath,shellPath]){const result=spawnSync(process.execPath,["--check",file],{encoding:"utf8"});if(result.status!==0){fs.writeFileSync(serverPath,original.server,"utf8");fs.writeFileSync(shellPath,original.shell,"utf8");fs.writeFileSync(htmlPath,original.html,"utf8");console.error("Part 131 rollback syntax failed; originals restored.");process.exit(1)}}console.log("Part 131 registration, module, aliases, bridge and progress removed.");
