# API Record

Public: `/api/part128/status`, `/security-policy`, `/catalog`, `/demo`.

Owner-only: preview, VANI command, confirm, cancel, actions history and records.

Models: Part128 action, Branch, Course, ClassBatch, TeacherProfile, StudentProfile, ParentStudentLink, StaffProfile and Audit.
